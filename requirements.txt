# RUNTIME dependencies — what the deployed app needs, and nothing more.
#
# torch, transformers, and sentence-transformers are deliberately ABSENT. They
# are build-time only (see requirements-build.txt). Serving loads SPECTER2
# through onnxruntime instead: ~50MB against torch's ~200MB, and an int8 model
# of 110MB against 440MB of fp32 weights. With torch the app measured ~834MB
# resident on Linux and OOMed on a 512MB instance; without it, ~340MB.

# Web app
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# LLM access — provider-agnostic, so CHATBOT_MODEL can point anywhere
litellm==1.93.0

# Query embedding. onnxruntime runs the exported model; tokenizers does the
# WordPiece step that transformers would otherwise be dragged in for.
onnxruntime==1.28.0
tokenizers==0.22.2
numpy==2.5.1

# Document handling — CV/paper uploads in, proposal .docx out
pypdf==6.14.2
python-docx==1.2.0

# Pipeline: scraping bio pages and querying OpenAlex / Semantic Scholar / CrossRef
requests==2.34.2
beautifulsoup4==4.15.0
