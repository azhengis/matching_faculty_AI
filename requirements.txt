# Web app
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# LLM access — provider-agnostic, so CHATBOT_MODEL can point anywhere
litellm==1.93.0

# Matching: SPECTER2 embeddings + cross-encoder rerank
sentence-transformers==5.6.0
transformers==5.14.1
torch==2.13.0
numpy==2.5.1

# Optional: SPECTER2 proximity adapter. Improves retrieval accuracy; search.py
# falls back to specter2_base when it is absent, so this is safe to omit on a
# constrained host (it pulls no extra weights, but the adapter download does).
# adapters==1.4.0

# Document handling — CV/paper uploads in, proposal .docx out
pypdf==6.14.2
python-docx==1.2.0

# Pipeline: scraping bio pages and querying OpenAlex / Semantic Scholar / CrossRef
requests==2.34.2
beautifulsoup4==4.15.0
